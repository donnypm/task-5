import React from "react";
import styles from "/styles/Home.module.scss";

const Header = () => {
  return (
    <header>
      <h1 className={styles.h1header}>List</h1>
    </header>
  );
};

export default Header;
